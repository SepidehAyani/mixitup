<main>
    <div class="p-3">
        <h2 class="text-center text-dark">Featured Drinks</h2>
        <div class="container animate__animated animate__fadeInUp">
            <ol class="container ">
                {{#each drinks as |drink|}}
                {{>catalog-cards}}
                {{/each}}
            </ol>
        </div>
    </div>
    </div>
</main>